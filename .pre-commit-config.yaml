repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.0  # keep in step with the ruff pin in pyproject.toml
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
