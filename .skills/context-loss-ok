# Judged losses from the #72 curation (AGENTS.md 6334 -> 5985).
# Each line below was read against its replacement before being warranted.
#
# The three demotions all removed prose whose content the destination doc already
# carries in a better form -- a table row, or a section written later and in more
# detail. Restoring the prose verbatim into those docs would have appended a
# near-duplicate beside the canonical treatment, which Phase 5 forbids, so these
# are warranted rather than relocated. The one line the destination did NOT carry
# (the REPLICATOR_ prefix rationale) was moved verbatim instead and is absent here.

# --- Server Lifecycle -> docs/DEPLOYMENT.md ---------------------------------
# Every fact verified present: the two merge paths and the guard (## Server
# Lifecycle table, ### The checkout guard), journalctl and the distinct consumer
# name (table rows), the cp/daemon-reload trap (its own paragraph), the override
# and the dev-worker question at the writer (#52).
duplicate :: pull when the merge happened here, because the guard refuses a `main` that is ahead
duplicate :: of `origin/main` (#48); debug with `sudo journalctl -u replicator -f`;
duplicate :: test a branch with `uv run python -m src.worker.main` under a distinct
duplicate :: `REPLICATOR_CONSUMER_NAME`. **After editing `deploy/replicator.service`, `cp` it to
duplicate :: `/etc/systemd/system/`** — the installed unit is a copy, not a symlink, so
duplicate :: `daemon-reload` alone silently re-reads the old file and the mismatch has no
duplicate :: symptom until a directive matters. **Refuses to start off `main`, or off
duplicate :: unpushed commits** (#37, #48) — `scripts/check_main_checkout.sh`;
duplicate :: question at the writer (#52).
retarget :: Full lifecycle table and the dev-server invocation:

# --- Environment Variables -> docs/ENVIRONMENT.md ---------------------------
# The boundary, the PAT list and the blast-radius reasoning are all in that doc's
# opening paragraph, which states them as one hard rule rather than as four.
duplicate :: Two env files, with a hard boundary between them:
duplicate :: 1. **`/etc/replicator/.env`** — production configuration. Survives repo resets
duplicate :: 2. **`.env`** (repo root, git-ignored) — dev/agent secrets: `GH_TOKEN` plus the per-repo PATs
duplicate :: **The service must never load the repo `.env`.** Those PATs carry org-wide write access
retarget :: Every variable the service reads, with the reasoning behind each default:

# --- The blob-backend bullet -> docs/STORAGE.md -----------------------------
# Backends, URI schemes, the sweep, the ceiling, customTime and the timeout are
# the rows of that doc's "Two backends, one seam" table.
duplicate :: - **Two blob backends, one seam, and the flip is not ours alone.** `local` announces
duplicate :: `file://` and `gcs` announces `gs://`; `local` is the compiled-in default and stays
duplicate :: `asyncio.to_thread` — which puts it in the unit's shutdown budget, not just the
duplicate :: handler's. Under `gcs` there is no sweep and `REPLICATOR_BLOB_MAX_TOTAL_BYTES`
duplicate :: is **not enforced** — retention is a bucket lifecycle rule on `customTime`, and
duplicate :: `blob_expires_at` becomes a floor. A missing blob is a `FileNotFoundError` on
duplicate :: both backends; every other provider failure carries its status for the caller
duplicate :: to classify (`is_terminal_provider_status`). Read
duplicate :: [docs/STORAGE.md](docs/STORAGE.md) before touching either store.

# These two said `local` "stays that way UNTIL a consumer can read the other
# scheme (CannObserv/watcher#275)". watcher#275 closed 2026-08-20 and the VM
# flipped the same day, yet the default did not move -- because docs/STORAGE.md
# records it as permanent: "Not a holding position until the cluster catches up
# -- a permanent one". The condition was met and the claim it gated is false.
disproven :: that way until a consumer can read the other scheme without an uncapped re-fetch
disproven :: loop (CannObserv/watcher#275). Every `BlobStore` call from a coroutine goes through

# --- The style bullets -> docs/STYLE.md -------------------------------------
# The block moved verbatim; this bold pseudo-heading became the `## General`
# heading of its own section, which is a heading rename the move forced.

# --- The 2026-09-09 curation (AGENTS.md 6,747 -> 5,969 exact tokens). ---
# Two shapes only. Every `tighten` line is a class-C rewrite whose claims all survive:
# prove-no-loss.sh --claims reports claims_dropped: 0, so no backticked identifier,
# issue reference or link target of the base file was dropped -- only words. The
# reasoning each rewrite stopped repeating was read in its destination first:
# docs/CONVENTIONS.md (OOM, ACL, the three fates, the dedupe keys), docs/STORAGE.md
# (the backend seam, to_thread inside the shutdown budget), docs/STREAMS.md (the
# compiled-in `local` default), docs/DEPLOYMENT.md (guard verdicts, the co-core pin).
# Some entries are re-wrapped fragments rather than whole claims -- a rewrite moves
# line boundaries, and whole-line matching cannot see the text survived.
#
# The `duplicate` five are the 'Where the reasoning lives' mini-index, which listed
# four docs the ## Detail Docs index below it already lists with fuller one-liners.
tighten :: Python ≥3.12, uv, pytest, ruff. `ty` is available as a **non-gating** type checker (`uv run ty check`) — advisory only; no pre-commit or CI gate.
tighten :: Auth is ADC. Pin the current minor — `>=0.13.1,<0.14` — and raise the **patch** floor with every co-core feature the code starts depending on: the reasoning, and the three ways a skew has already failed, in [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).
tighten :: **The manifest is a source, not the artifact.** Nothing re-embeds it — no hook, no CI step — so editing a `description` there changes what the repo says and not what `codebase_context_search` returns. Re-run `codebase_context_index` in the same change, or the highest-authority answer an agent gets stays the stale one (#19 CR #17).
tighten :: the `BlobStore` protocol — **two backends now** (`local`, `gcs`), selected by
tighten :: `REPLICATOR_BLOB_BACKEND` and defaulting to `local` (#7); `src/api/` is the dev-only
tighten :: `/health` app; `src/core/` holds config, logging, and the consume path's failure
tighten :: vocabulary. `tests/` mirrors
tighten :: `src/`. Every module with the job it owns:
tighten :: **Single-VM setup.** Code committed to main is the deployed code. Replicator shares the VM with archiver, watcher, and notifier.
tighten :: The worker binds no port; 8041 is the dev API port and 8040 is reserved. **Redis
tighten :: is Archiver-operated** — Replicator is a client, never ships a broker, never
tighten :: claims ownership — and server **≥ 7.0** is Replicator-critical because
tighten :: `claim_stale` reads `XAUTOCLAIM`'s three-element reply. `scripts/check_redis_floor.sh`
tighten :: guards it as an `ExecStartPre`. Ports, neighbours, and the redis-py pin:
tighten :: Three things that bite, each of which has no symptom until it matters:
tighten :: Every deploy situation with its command, the guard's full verdict table, and the
tighten :: dev-server invocation: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).
tighten :: Every variable, which file carries it, and the reasoning behind each default:
tighten :: Replicator is a **consumer** first. Follow the conventions co-core and the archiver producer established:
tighten :: `command_id:occurred_at`), so nothing an issuer waits on can collapse — storage
tighten :: identity and correlation identity are not interchangeable. `info_source_id`
tighten :: rides both and is **echoed, never read**, as is replicate's
tighten :: `info_item_rep_spec_id`: each `test_boundaries.py` carve-out is one field
tighten :: wide, and adding one edits the charter (#28, #29).
tighten :: `gs://`; `local` is the compiled-in default **by decision, not by schedule** —
tighten :: the deployment flipped on 2026-08-20 and the default did not. Every `BlobStore`
tighten :: call from a coroutine goes through `asyncio.to_thread` — which puts it in the
tighten :: unit's shutdown budget, not just the handler's. Per-backend retention, ceilings
tighten :: and failure classification: [docs/STORAGE.md](docs/STORAGE.md), which is the
tighten :: authority and worth reading before touching either store.
tighten :: - **Store, then publish — never the reverse.** A fact pointing at bytes that are
tighten :: not there is unrepairable by the consumer; stored bytes with no fact repair
tighten :: themselves on the reclaim.
tighten :: *before* it — as `XADD <topic>.dlq` then `XACK`, which is the form broker#2's
tighten :: ACL grants and is now observed rather than inferred (#79). Retry cadence is
tighten :: `REPLICATOR_CLAIM_MIN_IDLE_MS`; a failing *cycle* is `run_loop`'s problem, not
tighten :: the message's.
tighten :: ceiling, the consume path keeps reading, acking and reclaiming throughout, and
tighten :: nothing is dropped or dead-lettered. The third, `XGROUP CREATE … MKSTREAM`, is
tighten :: boot-only and does **not** retry: `ensure_group` re-raises anything but
tighten :: `BUSYGROUP`, so a first boot against a capped broker exits and systemd
tighten :: restarts. Verified against a scratch broker this repo spawns, never the shared
tighten :: one. Never answer an OOM with a client-level retry — a re-sent `XADD` the
tighten :: broker already applied publishes twice.
tighten :: `fetch_failed(handler_error)`. The cost is deliberate: a grant nobody fixes
tighten :: retries forever rather than reaching `<topic>.dlq`.
tighten :: handler — so losing them costs one TTL window of re-fetches and never
tighten :: correctness. Endorsed as bus state rather than a role blur, with broker#1's four
tighten :: answers and the ACL grant they imply, in
tighten :: - **The replicate loop writes for `gcs` (#29)** — create-if-absent, `blob_uri`
tighten :: never resolved as a path, writers keyed by alias, refusals before credentials,
tighten :: provider failures classified by HTTP status. Read
tighten :: [docs/CONVENTIONS.md](docs/CONVENTIONS.md) before touching that path.
tighten :: requires `--production` for the one combination the live worker consumes — a
tighten :: frame there is fetched for real.
tighten :: - **Three normative contracts bound the wire and the roadmap** — four documents,
tighten :: all under `docs/contracts/`, linked from sibling repos and indexed below.
tighten :: `tests/test_boundaries.py` enforces the charter in CI; change a
tighten :: charter and its tests together.
duplicate :: Where the reasoning lives:
duplicate :: - What each stream carries — [docs/STREAMS.md](docs/STREAMS.md)
duplicate :: - The rules common to all of them, and the `replicator:cmd:*` keyspace — [docs/CONVENTIONS.md](docs/CONVENTIONS.md)
duplicate :: - Blob paths, modes, and the retention sweep — [docs/STORAGE.md](docs/STORAGE.md)
duplicate :: - Fakeredis's divergences, the keys an integration run may touch, and why production `co-gcs-replication` is refused from every test — [docs/TESTING.md](docs/TESTING.md)
tighten :: The logging stack — one formatter, two installers, and the journald lines that
tighten :: are deliberately not JSON: [docs/STYLE.md](docs/STYLE.md).
tighten :: classes and functions, small focused functions, and tests mirroring source —
tighten :: each with its rationale and its ruff gate in [docs/STYLE.md](docs/STYLE.md).
tighten :: - [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — founding design and the module-by-module layout; read before changing one
tighten :: - [docs/CONVENTIONS.md](docs/CONVENTIONS.md) — the co-core/Redis Streams rules common to every stream: idempotency, validation, DLQ, `claim_stale`; and the `replicator:cmd:*` keys, this service's only non-stream footprint (#80)
tighten :: - [docs/STORAGE.md](docs/STORAGE.md) — blob paths and modes, the three populations under `REPLICATOR_BLOB_DIR`, TTL and ceiling semantics
tighten :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) — VM topology, ports, the systemd unit's lifecycle, and the co-core pin
tighten :: - [docs/TESTING.md](docs/TESTING.md) — where fakeredis diverges from the live broker, which keys an integration run may create, and why production `co-gcs-replication` is unreachable from every test (#38)
tighten :: - [docs/SKILLS.md](docs/SKILLS.md) — vendored skill inventory, refresh procedure, and the doc-check sensitive-path list
tighten :: - [docs/SOCRATICODE.md](docs/SOCRATICODE.md) — the full tool table, the prefetch query, per-tool gotchas, and cross-repo search
tighten :: - [docs/contracts/content-fetch-issuer-reference.md](docs/contracts/content-fetch-issuer-reference.md) — its lookup half: the refusal list, the failure taxonomy, the silent conditions, trust posture
tighten :: - [docs/contracts/content-replicate-issuer-contract.md](docs/contracts/content-replicate-issuer-contract.md) — the replicate trust model and issuer obligations, settled ahead of the code (#34)

# ---- #94 curation (AGENTS.md 6169 -> 5911) ----
#
# Every entry below was read against its replacement. `--claims` reports
# claims_dropped: 0 for this run, so no backticked span, issue reference or link
# target was lost by any of these rewrites -- only words were.
#
# The two broker-refusal bullets (OOM #79, ACL #82) collapsed into one. Both are
# carried in far more depth at docs/CONVENTIONS.md:26-33, which the tightened
# bullet now points at; restoring the prose there would append a near-duplicate
# beside the canonical treatment, which Phase 5 forbids.
#
# The DLQ bullet dropped its wire-form half (XADD/XACK, XDEL scope) -- carried
# whole at docs/CONVENTIONS.md:11. The blob-backend bullet dropped the
# shutdown-budget clause -- carried at docs/STORAGE.md:72-79.
#
# Project Layout was demoted to docs/ARCHITECTURE.md's own Project Layout
# section (6 lines relocated verbatim); the lines here are the pointer that
# replaced it.
tighten :: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).
tighten :: `scripts/check_redis_floor.sh`. Ports, redis-py pin: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).
tighten :: `gs://`; `local` stays the compiled-in default deliberately (#7).
tighten :: Every `BlobStore` call from a coroutine goes through `asyncio.to_thread`, which
tighten :: puts it in the unit's shutdown budget, not just the handler's.
tighten :: *before* it — as `XADD <topic>.dlq` then `XACK`, the form broker#2's ACL grants
tighten :: (#79). **Draining the queue is ours too**, by `XDEL` on `<topic>.dlq` and
tighten :: nowhere else (broker#12, #86). Retry cadence is
tighten :: - **A capped broker refuses only its `denyoom` commands, and the worker retries
tighten :: the two it meets at runtime (#79).** `XADD` and `SET` are refused and retried
tighten :: indefinitely — `OutOfMemoryError` is transient and exempt from the delivery
tighten :: ceiling — while the consume path reads, acks and reclaims throughout. The
tighten :: third, `XGROUP CREATE … MKSTREAM`, is boot-only and does **not** retry: a first
tighten :: boot against a capped broker exits and systemd restarts. Verified against a
tighten :: broker the tests spawn, **never the shared one**. Never answer an OOM with a
tighten :: client-level retry, which republishes an `XADD` the broker already applied.
tighten :: - **An ACL denial is transient too (#82).** `NoPermissionError` is the second
tighten :: `ResponseError` subclass in `_TRANSIENT_ERRORS`, so a grant broker#1's cutover
tighten :: got wrong backs off instead of closing valid commands with a terminal
tighten :: `fetch_failed(handler_error)` — at the deliberate cost that a grant nobody
tighten :: fixes retries forever.
tighten :: under `docs/contracts/`, linked from sibling repos and indexed below.
tighten :: tests together.
tighten :: **Logging:**
tighten :: ```python
tighten :: from src.core.logging import get_logger
tighten :: logger = get_logger(__name__)
tighten :: **General:** imports at file top and explicit, docstrings on public modules,
tighten :: with its rationale and ruff gate in [docs/STYLE.md](docs/STYLE.md).
retarget :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) — VM topology, ports, the unit's lifecycle, the co-core pin

# ---- #94 curation, the docs/DEPLOYMENT.md -> docs/INFRASTRUCTURE.md split ----
#
# Scoped to that file: these are judged for the split run, not the policy run.
# `--claims` reports claims_dropped: 0, so the split carried every atom across.
#
# The intro described the doc's whole scope, and half that scope left in this
# same change; the replacement names what stayed and links what went. The
# heading became the new document's own title, which is what a split does.
docs/DEPLOYMENT.md :: retarget :: Dedicated-VM topology, the systemd unit's lifecycle and the guards it starts
docs/DEPLOYMENT.md :: retarget :: behind, and the co-core pin. `AGENTS.md` keeps the two-env-file boundary and the
docs/DEPLOYMENT.md :: retarget :: restart command; the reasoning behind each of them is here. The variables
docs/DEPLOYMENT.md :: retarget :: [ENVIRONMENT.md](ENVIRONMENT.md).
docs/DEPLOYMENT.md :: rename :: ## Infrastructure

# ---- #94 CR 18 ----
# The bullet's opening line was rewritten in place to put REPLICATOR_BLOB_BACKEND
# back in the policy file — a class-A constraint the curation had demoted inside
# overview prose. `tighten` is the mechanical warrant (rewritten in place, atoms
# preserved, claims_dropped: 0); note it is the unusual direction, adding a word
# rather than removing one, which is the point of the fix.
tighten :: - **Two blob backends, one seam.** `local` announces `file://` and `gcs` announces
# Phase 6.5 renamed this heading after the split's no-loss run had already
# passed, so the warrant was missed at the time: `#\d{2,}` in a permanent anchor
# slug is a class 3b seam, and ENVIRONMENT.md's link was retargeted with it.
docs/DEPLOYMENT.md :: rename :: ### When the worker fails, who is told (#94)

# ---- #92 curation (AGENTS.md 6153 -> 5991 exact tokens) ----
#
# Eleven class-C rewrites in place, no demotion: `--claims` reports
# claims_dropped: 0 against 149 atoms, so not one backticked span, issue
# reference or link target of the base file was dropped -- only words were. Each
# line below was read against its replacement.
#
# What the rewrites stopped repeating, and where it still lives:
#   * The replicate-loop bullet's five-item list was a table of contents for
#     docs/CONVENTIONS.md, which the same bullet tells you to read first.
#   * The two broker-refusal pointers ("which commands are refused, why each
#     classification, and the costs both carry") named the sections of
#     docs/CONVENTIONS.md they point at.
#   * The two systemd units share one rule -- copy, not symlink -- so it is
#     stated once and both units named under it. Every atom kept, including
#     `journalctl -t replicator-failure` and the `%n` gotcha.
#   * "Three normative contracts ... four documents" needed the ## Detail Docs
#     index to disambiguate, and that index is the next section.
#   * The wheelhouse sync COMMAND was verbatim twice in this file; the copy
#     under Common Commands went and its comment now points at the one that
#     kept its reasoning. Only the comment is a loss; the command is still
#     inline under Environment & Tooling.
#   * "# these do not exercise all of src/" restated the --no-cov rationale
#     given one command above it.
tighten :: **Worker-first.** Primary process = bus consumer (`src/worker/main.py`), not an HTTP API. The FastAPI app is a `/health` surface only, dev-only until a status endpoint is wanted.
tighten :: **Cap anything that launches a SocratiCode server** — uncapped, one cost this cluster 58 min of bus (#94). Invocations in [docs/COMMANDS.md](docs/COMMANDS.md). A green `codebase_search` is also not evidence every linked sibling answered: three silent-skip modes, and which sibling is in one today, in [docs/SOCRATICODE.md](docs/SOCRATICODE.md).
tighten :: - **`/etc/systemd/system/replicator.service` is a copy, not a symlink** — `cp` it
tighten ::   after every edit to `deploy/`, because `daemon-reload` alone re-reads the old file.
tighten ::   **Two unit files now** (#94): `replicator-failure-notify@.service` is the
tighten ::   `OnFailure=` handler, copied the same way, and a missed `cp` stays invisible until
tighten ::   the first failure. Read what it reported with `journalctl -t replicator-failure` —
tighten :: For shell commands (dev only), load both — the snippet is under Common Commands.
tighten ::   `info_item_rep_spec_id` are **echoed, never read** — each `test_boundaries.py`
tighten ::   [docs/STORAGE.md](docs/STORAGE.md) is the authority — read it before touching
tighten ::   either store.
tighten ::   republishes an `XADD` the broker already applied. Which commands are refused,
tighten ::   why each classification, and the costs both carry:
tighten ::   [docs/CONVENTIONS.md](docs/CONVENTIONS.md).
tighten :: - **The replicate loop writes for `gcs` (#29)** — create-if-absent, `blob_uri` never
tighten ::   resolved as a path, writers keyed by alias, refusals before credentials, provider
tighten ::   failures classified by HTTP status. Read
tighten ::   [docs/CONVENTIONS.md](docs/CONVENTIONS.md) first.
tighten :: - **Three normative contracts bound the wire and the roadmap** — four documents
tighten ::   under `docs/contracts/`, indexed below. `tests/test_boundaries.py` enforces the
tighten ::   charter in CI; change a charter and its tests together.
tighten :: # Mirror the private index, then install
tighten :: # Integration tests (a scratch redis-server, or one the test spawns; --no-cov —
tighten :: # these do not exercise all of src/)

# ---- #92 curation, second pass to reach the budget exactly ----
# The exact count (6015) sat 15 over where the offline estimate had read 5991,
# so four more lines were tightened. `--claims` still reports claims_dropped: 0.
#
# "Red -> Green -> Refactor" restates what "TDD required" already says and what
# the model already knows -- the one `default` warrant in this run. The three
# index lines lost a conjunction, a repeated "and", and the word "normative",
# which the Bus Conventions bullet above now states once for all four contracts.
default :: TDD required. Red → Green → Refactor. No production code without a failing test first.
tighten :: - [docs/CONVENTIONS.md](docs/CONVENTIONS.md) — the rules common to every stream: idempotency, validation, DLQ, `claim_stale`; and the `replicator:cmd:*` keys (#80)
tighten :: - [docs/STYLE.md](docs/STYLE.md) — the logging stack: formatter, installers, and the non-JSON journald lines
tighten :: - [docs/contracts/content-fetch-issuer-contract.md](docs/contracts/content-fetch-issuer-contract.md) — what a `content.fetch` producer must do; normative, linked from issuer repos

# ---- #92 docs curation (the five over-budget reference docs) ----
# Every line below is a class-C rewrite in place; --claims passes with the four
# judged atom drops in .skills/context-claims-ok. Two shapes:
#
#   docs/contracts/replicator-boundaries.md — the passage said Watcher still
#   owed the issuer half of the policy stream and called a deleted file
#   "load-bearing today". watcher#245 closed 2026-08-06 and the file 404s;
#   both checked against the API, not the stale clone on this VM.
#
#   docs/CONVENTIONS.md — a stale VM gloss and two asides restating
#   docs/DEPLOYMENT.md's subject.
docs/CONVENTIONS.md :: tighten :: - **A consumer *group's* name is co-core's to define, and both defaults derive it (#77, cannobserv#384).** `<service>.<stream-suffix>[-<purpose>]` via `group_name(topic, service)`, so `content.fetch` → `replicator.fetch` and `content.replicate` → `replicator.replicate` — the names live on the broker, audited 2026-09-01. `src/core/config.py` calls the helper rather than writing the strings, because a literal here is a second copy of a rule this repo does not own, and the cluster's group naming has already drifted once. The failure that forecloses is the quiet one: a renamed convention spelled locally as a literal reaches `ensure_group`, which creates a **new empty group beside the real one**, and the worker then polls a stream nothing delivers to. `purpose` is omitted while a service runs one group per stream and disambiguates when it runs more. The floor is co-core `>=0.13.1`, and `uv.lock` plus `ExecStart`'s `--frozen --no-sync` mean a convention change cannot arrive on a deploy without a lockfile commit — which runs the tests that catch it. Config/state streams have no group at all: `group_name` raises on `content.fetch-policy`, and `stream_kind` is the machine-readable form of the three-kind taxonomy this file's rules are organised around.
docs/CONVENTIONS.md :: tighten :: - **A consumer's name is derived from its group, never from the host (#77).** `<group with dots dashed>-<slot>`, so `replicator.fetch` → `replicator-fetch-1` and `replicator.replicate` → `replicator-replicate-1`, one per group and computed at the wiring seam (`src/worker/main.py::consumer_name_for`). The rule is **stability**: a registration persists until an explicit `XGROUP DELCONSUMER` that nothing calls, so a name carrying anything that varies — a hostname, a pid — mints a fresh registration on each change and abandons the old one holding its PEL, reclaimable only by an `XAUTOCLAIM` at `min_idle_time`. Archiver reached seven registrations on the production broker, six dead (archiver#156); Replicator's hostname-derived spelling additionally read as the *Watcher* service's, this VM being named `watcher`. A stable name makes a restart reuse its registration, so the leak cannot recur — no periodic sweep, and no shutdown hook a `SIGKILL` would skip. Overrides (`REPLICATOR_CONSUMER_NAME`, `REPLICATOR_REPLICATE_CONSUMER_NAME`) are **per group** for the same reason the name is: one process-wide override put a `replicator-fetch-…` consumer inside `replicator.replicate`, a name that misstates its own group. The `-1` is a slot — a second member of a group takes `-2` upward, and must not share a name with the first, since Redis tracks pending entries per consumer name.
docs/contracts/replicator-boundaries.md :: tighten :: `DEFAULT_MIN_INTERVAL = 1.0` s (`src/core/rate_limiter.py`), backing off to
docs/contracts/replicator-boundaries.md :: tighten :: `BACKOFF_MAX_INTERVAL = 60.0` s. So parking matches the *backoff* case almost exactly and
docs/contracts/replicator-boundaries.md :: tighten :: **The stream is a precondition of the Phase 4 cutover, not a follow-on to it.** Watcher's limiter
docs/contracts/replicator-boundaries.md :: tighten :: (`src/core/rate_limiter.py::acquire_for_domain`, fed by 429s its own fetch path observes) is
docs/contracts/replicator-boundaries.md :: tighten :: load-bearing today and stops functioning the moment that fetch path becomes a publish path — it does
docs/contracts/replicator-boundaries.md :: tighten :: not fail, it silently becomes decorative, pacing publication rather than origin requests. #12's default closed that window on the consumer side and #19
docs/contracts/replicator-boundaries.md :: tighten :: supplies the numbers; **what remains is issuer-side** — Watcher publishing its `Domain` rows
docs/contracts/replicator-boundaries.md :: tighten :: onto this stream, tracked at
docs/contracts/replicator-boundaries.md :: tighten :: [CannObserv/watcher#245](https://github.com/CannObserv/watcher/issues/245). Until it does,
docs/contracts/replicator-boundaries.md :: tighten :: every host resolves to the fallback, which is the pre-#19 behaviour and is why the consumer
docs/contracts/replicator-boundaries.md :: tighten :: half could land first.

# --- #92 split: content-fetch-issuer-reference.md -> + content-fetch-outcome-reference.md ---
# The doc was 12,564 tokens (2,564 over the per-doc budget) and the only one of the
# five over-budget docs with no citation outside this repo, so it is the one that
# could be restructured without breaking a sibling's link. It split on the direction
# of the traffic: the request side stayed here (5,210), the result side moved to
# content-fetch-outcome-reference.md (8,142). 228 lines relocated verbatim.
#
# Every line below is a POINTER this same change invalidated, read against its
# replacement before being warranted:
#
#   * The "What is here rather than there" paragraph described a two-file family.
#     There are now three files, and the enumeration it gives ("the reasoning behind
#     the enriched blob_available fields, the condition-by-condition failure
#     taxonomy") names content that is no longer in the file the paragraph is in.
#     Rewritten in place to say which half is where; "both files are normative"
#     became "all three are normative", which is the same rule over a larger set.
#   * "a change to the failure taxonomy, to the refusal rules" -- the failure taxonomy
#     moved out, and the outcome half states its own announcement rule. This copy
#     now reads "to the refusal rules, to the trust posture".
#   * "the blob_available passthroughs take", "the request-options set above" and
#     "which is the row above" -- three intra-document references that became
#     cross-document ones. Each is now a link to the section's new home; the
#     continuation lines are lost only because the rewrap moved the break points.
docs/contracts/content-fetch-issuer-reference.md :: retarget :: the payload shapes, the eight MUSTs, and the guarantee/non-guarantee pair. This file carries
docs/contracts/content-fetch-issuer-reference.md :: retarget :: what an issuer *looks up* — the header and timeout rules it consults when a command is refused,
docs/contracts/content-fetch-issuer-reference.md :: retarget :: the reasoning behind the enriched `blob_available` fields, the condition-by-condition failure
docs/contracts/content-fetch-issuer-reference.md :: retarget :: taxonomy, the trust posture the whole capability rests on, and the mechanisms behind several of
docs/contracts/content-fetch-issuer-reference.md :: retarget :: the MUSTs, each of which states its rule there and explains itself here. The split exists so the
docs/contracts/content-fetch-issuer-reference.md :: retarget :: contract stays short enough to read start to finish; both files are normative, and a rule does not
docs/contracts/content-fetch-issuer-reference.md :: retarget :: become advisory by living here.
docs/contracts/content-fetch-issuer-reference.md :: retarget :: here as well as the rules: a change to the failure taxonomy, to the refusal rules, or to what an
docs/contracts/content-fetch-issuer-reference.md :: retarget :: one the `blob_available` passthroughs take: a header Replicator silently dropped, or a timeout it
docs/contracts/content-fetch-issuer-reference.md :: retarget :: refuses the command before contacting the origin — both worse than none. The unsendable set is the request-options set above: anything outside printable
docs/contracts/content-fetch-issuer-reference.md :: retarget :: US-ASCII, interior HTAB and obs-text included.
docs/contracts/content-fetch-issuer-reference.md :: retarget :: - **A destination guard instead** (#95), which is the row above. It bounds where a fetch may
docs/contracts/content-fetch-issuer-reference.md :: retarget :: *point* rather than who may issue one, because those are different questions and only the second
docs/contracts/content-fetch-issuer-reference.md :: retarget :: was ever answered by a grant.

# --- #92 split: the AGENTS.md Detail Docs roster ---------------------------
# The split added a 26th reference doc, and AGENTS.md was at exactly 6,000/6,000
# with no margin, so the new index entry had to be paid for rather than appended.
# Every entry in the roster spelled its target twice -- once as the link LABEL and
# again as the link TARGET -- so the label was reduced to the basename. No claim
# and no atom is dropped: the full path is still in every target, which is what
# both the link graph and check-seams read. Read against the replacement line by
# line; the descriptions are untouched.
#
# The issuer-reference entry is the exception and is warranted `retarget`: half of
# what it described -- the failure taxonomy and the silent conditions -- moved into
# content-fetch-outcome-reference.md in this same change, so the roster now carries
# one line per doc, as it does for every other entry. Nothing it named was dropped;
# both halves are named, each beside the file that now holds it.
AGENTS.md :: tighten :: - [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — founding design, the command → fact flow, module by module; read before changing one
AGENTS.md :: tighten :: - [docs/STREAMS.md](docs/STREAMS.md) — what each stream carries, one bullet per rule `AGENTS.md` states in a line
AGENTS.md :: tighten :: - [docs/CONVENTIONS.md](docs/CONVENTIONS.md) — the rules common to every stream: idempotency, validation, DLQ, `claim_stale`, and the `replicator:cmd:*` keys (#80)
AGENTS.md :: tighten :: - [docs/STORAGE.md](docs/STORAGE.md) — blob paths and modes, the populations under `REPLICATOR_BLOB_DIR`, TTL and ceilings
AGENTS.md :: tighten :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) — the unit's lifecycle, its start guards, what it reports when it fails, the co-core pin
AGENTS.md :: tighten :: - [docs/INFRASTRUCTURE.md](docs/INFRASTRUCTURE.md) — VM topology, ports, the broker, and the buckets either side of the test/production line
AGENTS.md :: tighten :: - [docs/reference/tailscale.md](docs/reference/tailscale.md) — this node: tailnet, ACL, DNS, broker latency
AGENTS.md :: tighten :: - [docs/ENVIRONMENT.md](docs/ENVIRONMENT.md) — every variable either env file carries, and the boundary between them
AGENTS.md :: tighten :: - [docs/TESTING.md](docs/TESTING.md) — fakeredis's divergences, the keys an integration run may create, why production `co-gcs-replication` is unreachable (#38)
AGENTS.md :: tighten :: - [docs/STYLE.md](docs/STYLE.md) — the logging stack: formatter, installers, the non-JSON journald lines
AGENTS.md :: tighten :: - [docs/COMMANDS.md](docs/COMMANDS.md) — every runnable command, with flags
AGENTS.md :: tighten :: - [docs/SKILLS.md](docs/SKILLS.md) — vendored skill inventory, refresh procedure, doc-check lists
AGENTS.md :: tighten :: - [docs/SOCRATICODE.md](docs/SOCRATICODE.md) — full tool table, prefetch query, per-tool gotchas, cross-repo search
AGENTS.md :: tighten :: - [docs/contracts/content-fetch-issuer-contract.md](docs/contracts/content-fetch-issuer-contract.md) — what a `content.fetch` producer must do; linked from issuer repos
AGENTS.md :: retarget :: - [docs/contracts/content-fetch-issuer-reference.md](docs/contracts/content-fetch-issuer-reference.md) — its lookup half: refusal list, failure taxonomy, silent conditions, trust posture
AGENTS.md :: tighten :: - [docs/contracts/replicator-boundaries.md](docs/contracts/replicator-boundaries.md) — what Replicator may become; run its three tests against any proposed capability
AGENTS.md :: tighten :: - [docs/contracts/content-replicate-issuer-contract.md](docs/contracts/content-replicate-issuer-contract.md) — the replicate trust model and issuer obligations (#34)

# --- #99 curation: DEPLOYMENT.md split + AGENTS.md headroom -------------
# duplicate: docs/ARCHITECTURE.md ## Project Layout already carries these,
#   `tests/` mirrors `src/`. verbatim; AGENTS.md keeps an orienting line + pointer.
# tighten:   rewritten in place, atoms carried across and checked by --claims.
tighten :: **The manifest is a source, not the artifact.** Nothing re-embeds it — run `codebase_update` in the same change as a `description` edit, or the stalest answer carries the most authority (#19 CR #17). Not `codebase_context_index` (skills#317).
duplicate :: `src/worker/` is the primary process — the bus consumer. `src/storage/` is the
duplicate :: content-addressed temp store; `src/api/` is the dev-only `/health` app;
duplicate :: `src/core/` holds config, logging, and the consume path's failure vocabulary;
duplicate :: `tests/` mirrors `src/`. The seams each module sits behind, and every module with
tighten :: the job it owns: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).
tighten :: - **Both installed units are copies, not symlinks** — `cp` after every edit to
tighten ::   `deploy/`, because `daemon-reload` alone re-reads the old file.
tighten ::   `/etc/systemd/system/replicator.service`, and since #94 the `OnFailure=` handler
tighten ::   `replicator-failure-notify@.service`, whose missed `cp` stays invisible until the
tighten ::   first failure. Read what it reported with `journalctl -t replicator-failure`;
tighten ::   `journalctl -u` finds nothing, because `%n` doubles the suffix.
tighten :: worker has no use for, and a process whose job is fetching public URLs must not
tighten :: widen their blast radius. Anything the service needs goes in `/etc/replicator/.env`.
# #99 CR 12-15: the DEPLOYMENT.md index line was edited in place when the
#   FAILURE-NOTIFICATION.md entry was added beside it. NOT `index`: that warrant
#   is for a line EXTENDED in place, and this one dropped a clause. The clause
#   'what it reports when it fails' described content this same change moved to
#   docs/FAILURE-NOTIFICATION.md, which the line immediately below now links.
retarget :: - [DEPLOYMENT.md](docs/DEPLOYMENT.md) — the unit's lifecycle, its start guards, what it reports when it fails, the co-core pin
# #111: per-host politeness split into docs/POLITENESS.md out of STREAMS.md,
#   CONVENTIONS.md and the charter. Each line below is a pointer or a
#   self-reference whose target moved in the same change; the moved text
#   itself arrived verbatim.
docs/STREAMS.md :: retarget :: - **Politeness is enforced here and decided elsewhere, and since #19 the numbers actually arrive.**
docs/contracts/replicator-boundaries.md :: retarget :: - **enforcement has a rate floor of `REPLICATOR_CLAIM_MIN_IDLE_MS`** (default 60 s). See below.
docs/contracts/replicator-boundaries.md :: retarget :: rides the worker's stop event, because how long it runs is the producer's business: this
docs/contracts/replicator-boundaries.md :: retarget :: document asks the producer to `MAXLEN`, and Replicator cannot enforce it.
docs/contracts/replicator-boundaries.md :: retarget :: **Resolved by splitting the wait by duration**, and shipped with the interim default below:
docs/contracts/replicator-boundaries.md :: retarget :: absorbed rather than fatal: [STREAMS.md](../STREAMS.md).
