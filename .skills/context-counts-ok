# Counts judged legitimate — check-counts.sh --ack-file default.
# First pass, 2026-09-09: this check post-dates the repo's last curation, so
# every entry below is an existing count read for the first time. Two counts
# were dropped rather than warranted (form 2, the policy-file default): the
# "three ways a skew has already failed" now reads "the ways", and the logging
# stack's "one formatter, two installers" now reads "its formatter, its
# installers" — both numbers were rhetorical and neither is checkable here.
#
# Pinned 2026-09-13 (#91): every entry names the file it was judged against. This
# file is per-repo and a run is per-file, so an unpinned entry was reported
# ambiguous on every --file run against another doc (skills#279). Pinned, it sits
# those runs out, and when its own text changes it is reported stale, not
# ambiguous — the expiry the grammar promises (skills#251).
#
# --- enumerated: the counted things are named in the same clause or the list
# --- immediately under it, so the number is checkable by reading the section.
AGENTS.md :: enumerated :: **two backends** (`local`, `gcs`)
AGENTS.md :: enumerated :: Three that bite, each symptomless until it matters:
AGENTS.md :: enumerated :: Two env files, and the boundary between them
AGENTS.md :: enumerated :: **Two blob backends, one seam.**
AGENTS.md :: enumerated :: the worker retries the two it meets at runtime
AGENTS.md :: enumerated :: **Three stream kinds, three sets of rules.**
# The three contracts and their four documents are the four `docs/contracts/`
# rows in ## Detail Docs, one line down the same file.
AGENTS.md :: enumerated :: **Three normative contracts bound the wire and the roadmap**
AGENTS.md :: enumerated :: four documents under `docs/contracts/`
#
# --- stable: structural, and cannot change without the sentence being rewritten.
# XAUTOCLAIM's reply shape is Redis wire format, and the ≥7.0 floor beside it is
# what the sentence exists to state; a fourth element would rewrite both.
AGENTS.md :: stable :: `XAUTOCLAIM`'s three-element reply
# The same clause in DEPLOYMENT.md's Redis-floor paragraph, which the AGENTS.md
# line summarises; the unpinned entry acknowledged both. Re-read there for #91:
# the same warrant holds, and that sentence names the third element outright.
docs/DEPLOYMENT.md :: stable :: `XAUTOCLAIM`'s three-element reply
