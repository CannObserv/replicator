# Judged atom drops. An entry names WHY a tightening did not carry a claim
# across; the atom is written as prove-no-loss.sh reports it, without backticks.

# ---- #92 docs curation ----
# The co-core floor, stated once in docs/DEPLOYMENT.md (verified present there)
# rather than twice; CONVENTIONS.md now points at it.
duplicate :: >=0.13.1
# The bullet explained a past incident with "this VM being named `watcher`".
# #88 moved Replicator to co-replicator, tailnet `replicator`, so the gloss
# named a host this repo has not run on since. The incident itself is kept.
disproven :: watcher
# Both rate_limiter atoms: the file 404s on CannObserv/watcher's API — deleted
# with the Phase 4 cutover, replaced by src/core/fetch_policy.py. One of the two
# was cited as "load-bearing today".
disproven :: src/core/rate_limiter.py::acquire_for_domain
disproven :: src/core/rate_limiter.py
# #99 curation: survives as `deploy/replicator-failure-notify@.service` in
#   docs/DEPLOYMENT.md (cp table + line 53) and docs/COMMANDS.md.
duplicate :: replicator-failure-notify@.service
# #111: the backticked label of the removed "Why the issuer renders" pointer
#   section. The same plan file is still linked twice in the contract — T3's
#   rationale link and "Where the rest of the contract lives".
docs/contracts/content-replicate-issuer-contract.md :: duplicate :: docs/plans/2026-08-14-why-the-issuer-renders-settled.md
# #111: link atoms retargeted by the content.fetch family rebalance — each
#   section they named moved in the same change, and the new link names it.
docs/contracts/content-fetch-issuer-contract.md :: retarget :: #8-do-not-send-a-validator-until-you-handle-not_modified
docs/contracts/content-fetch-issuer-contract.md :: retarget :: content-fetch-outcome-reference.md#pacing-at-the-deployed-defaults
docs/contracts/content-fetch-outcome-reference.md :: retarget :: content-fetch-issuer-contract.md#the-success-fact
docs/contracts/content-fetch-outcome-reference.md :: retarget :: content-fetch-issuer-contract.md#the-failure-fact
# #111: atoms of the refuted hook and driver claims (see context-loss-ok).
AGENTS.md :: default :: "$SOCRATICODE_DRIVER"
AGENTS.md :: disproven :: git status -sb
docs/SKILLS.md :: disproven :: UNKNOWN
docs/SKILLS.md :: default :: node "$SOCRATICODE_DRIVER"
docs/SKILLS.md :: disproven :: node skills/init-socraticode/scripts/mcp-driver.mjs
docs/SKILLS.md :: default :: settings.local.json
docs/SKILLS.md :: disproven :: skills/init-socraticode
